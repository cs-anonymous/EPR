# CPT数据策略分析

## 📊 当前CPT数据集（language_cpt_s1）

### 数据组成
| 语料库 | 原始行数 | 采样比例 | 输出行数 | 占比 |
|--------|---------|---------|---------|------|
| **midi_tsv_no_header** | 425,739 | 1.0x | 425,739 | 88.8% |
| **aligned_abcx** | 25,484 | 1.5x | 38,226 | 8.0% |
| **knowledge_Seeker38** | 47,456 | 0.3x | 14,237 | 3.0% |
| **knowledge_format** | 24 | 50x | 1,200 | 0.2% |
| **总计** | 498,703 | - | **479,402** | 100% |

### 数据特点
- **MIDI数据占主导**：88.8%
- **ABC记谱**：8.0%（已过采样1.5倍）
- **知识数据**：3.2%（欠采样）
- **格式示例**：0.2%（大量重复50次）

### 当前训练配置
- **Epochs**: 1.0
- **总步数**: 14,982
- **当前进度**: 63% (9,505/14,982)
- **剩余**: 5,477步

## 🤔 是否需要添加更多数据？

### 判断标准

#### 1. **Loss是否饱和？** ❌ 还没有
- 当前loss: 1.506 → 1.467
- 还在下降，没有饱和
- **结论：先完成当前epoch**

#### 2. **数据是否充足？** ⚠️ 取决于目标
```
当前数据量：479,402 条
训练1 epoch：479,402 条
训练2 epochs：958,804 条
训练3 epochs：1,438,206 条
```

**对比：**
- GPT-3: 300B tokens
- LLaMA: 1.4T tokens
- 你的CPT: ~479K samples × 平均长度

**结论：数据量不算大，但对于CPT（适应新tokens）可能够用**

#### 3. **数据多样性如何？** ⚠️ 可以改进
- MIDI数据占88.8%（过于单一）
- ABC记谱只有8%
- 知识数据只有3%
- **可能导致模型偏向MIDI格式**

## 💡 策略建议

### 方案A：先完成当前训练，再评估（推荐）✅

**步骤：**
1. **完成当前1 epoch训练**（今晚19:00）
2. **评估模型表现**
   - 在验证集上测试loss
   - 在下游任务上测试（如果有）
   - 看是否达到预期

3. **根据结果决定：**
   - ✅ 如果效果好 → 进入SFT阶段
   - ⚠️ 如果效果一般 → 考虑方案B或C
   - ❌ 如果效果差 → 考虑方案D

**优点：**
- 不浪费当前训练
- 有数据支持决策
- 避免盲目增加数据

### 方案B：增加epochs（如果loss未饱和）

**适用场景：**
- 1 epoch后loss还在下降
- 没有过拟合迹象
- 数据质量好

**操作：**
```bash
# 修改训练脚本
--num-train-epochs 2.0  # 或 3.0

# 从最后的checkpoint继续
--resume-from-checkpoint output/language-cpt-s1-r32-hf-1536/checkpoint-14500
```

**优点：**
- 简单直接
- 充分利用现有数据
- 可能进一步降低loss

**缺点：**
- 可能过拟合
- 数据多样性不变

### 方案C：添加更多数据（推荐，如果需要改进）⭐

#### C1. 增加数据多样性

**可以添加的数据：**

1. **更多MIDI数据**
   - 如果有更多MIDI数据集
   - 增加MIDI的多样性（不同风格、乐器）

2. **CoreA数据**（你提到的）
   - 如果CoreA包含不同类型的音乐数据
   - 可以增加数据多样性

3. **更多ABC记谱**
   - 当前只有25K条
   - 可以增加到50K-100K

4. **更多知识数据**
   - 当前只采样了30%
   - 可以增加到50%-100%

5. **其他格式**
   - MusicXML
   - LilyPond
   - 其他音乐表示

**建议的数据配比：**
```
MIDI:        60-70%（降低，避免过度偏向）
ABC:         15-20%（增加）
Knowledge:   10-15%（增加）
Other:       5-10%（新增）
```

#### C2. 创建 language_cpt_s2

**步骤：**
```bash
# 1. 收集更多数据
# 2. 调整采样比例
# 3. 创建新的数据集
python scripts/build_language_cpt_s2.py \
  --midi-ratio 0.65 \
  --abc-ratio 0.20 \
  --knowledge-ratio 0.15

# 4. 从当前checkpoint继续训练
torchrun --nproc_per_node=4 \
  scripts/train_cpt_hf_peft.py \
  --dataset PianoCoReS/Corpora/language_cpt/language_cpt_s2_train.jsonl \
  --resume-from-checkpoint output/language-cpt-s1-r32-hf-1536/checkpoint-14500 \
  --num-train-epochs 1.0
```

**优点：**
- 增加数据多样性
- 避免过拟合
- 模型更泛化

**缺点：**
- 需要准备新数据
- 训练时间更长

### 方案D：改进数据质量（如果效果差）

**可能的问题：**
1. **数据清洗不够**
   - MIDI数据有噪声
   - ABC格式不统一

2. **数据不平衡**
   - MIDI占88.8%太多
   - 其他类型太少

3. **数据标注问题**
   - 格式不一致
   - 缺少必要信息

**操作：**
- 重新清洗数据
- 调整采样比例
- 增加数据验证

## 🎯 我的推荐

### 短期（现在）：
1. ✅ **完成当前1 epoch训练**
2. ✅ **评估模型效果**
3. ✅ **记录loss曲线和最终loss**

### 中期（训练完成后）：
1. **如果loss降到1.3-1.4并稳定**
   - → 进入SFT阶段
   - → 在下游任务上评估

2. **如果loss还在1.4-1.5徘徊**
   - → 考虑再训练1-2个epochs
   - → 或者调整学习率

3. **如果想进一步改进**
   - → 创建 language_cpt_s2
   - → 增加数据多样性
   - → 调整数据配比

### 长期（如果需要）：
1. **收集更多高质量数据**
   - CoreA数据
   - 更多MIDI数据
   - 其他音乐格式

2. **改进数据配比**
   - 降低MIDI占比（60-70%）
   - 增加其他类型（30-40%）

3. **多阶段CPT**
   - Stage 1: 适应新tokens（当前）
   - Stage 2: 增加数据多样性
   - Stage 3: 特定领域微调

## 📈 数据量参考

### 典型CPT数据量：
- **小规模CPT**: 100K-1M samples
- **中规模CPT**: 1M-10M samples
- **大规模CPT**: 10M+ samples

### 你的情况：
- **当前**: 479K samples × 1 epoch = 479K
- **如果2 epochs**: 479K × 2 = 958K ✅ 合理
- **如果3 epochs**: 479K × 3 = 1.4M ✅ 也可以

### 建议：
- **最少**: 1 epoch（当前）
- **推荐**: 2-3 epochs（如果loss未饱和）
- **最多**: 5 epochs（避免过拟合）

## 🔬 如何判断是否需要更多数据？

### 信号1：Loss曲线
```
✅ 需要更多epochs：loss持续下降
⚠️ 需要更多数据：loss饱和但验证loss高
❌ 不需要：loss饱和且验证loss低
```

### 信号2：验证集表现
```
✅ 需要更多数据：训练loss低，验证loss高（过拟合）
⚠️ 需要更多epochs：训练和验证loss都在下降
❌ 不需要：验证loss已经很低
```

### 信号3：下游任务表现
```
✅ 需要更多数据：CPT后SFT效果不好
⚠️ 需要改进数据质量：loss低但任务效果差
❌ 不需要：任务效果已经满意
```

## 结论

### 现在不要急着添加数据！

1. **先完成当前训练**（今晚19:00）
2. **评估效果**（loss、验证集、下游任务）
3. **根据评估结果决定**：
   - 效果好 → 进入SFT
   - 效果一般 → 再训练1-2 epochs
   - 效果差 → 考虑添加数据或改进数据质量

### 如果确实需要添加数据：
- 优先增加**数据多样性**（ABC、知识、其他格式）
- 而不是单纯增加MIDI数据量
- 调整数据配比：MIDI 60-70%, 其他 30-40%

---

**更新时间：** 2026-05-23 07:40
**当前进度：** 63% (9505/14982)
**预计完成：** 今晚 19:00
**下一步：** 等待训练完成，评估效果
